# Instagram Reels Dashboard - Web App

Веб-дашборд для анализа Instagram Reels.

## Возможности

- 📊 Визуализация статистики Reels
- 📅 Выбор периода анализа
- 📈 Графики просмотров, ER, лайков
- 🔗 Быстрый доступ к постам

## Локальный запуск

1. Скопируйте `env.example` в `.env`:
```bash
cp env.example .env
```

2. Заполните `.env` вашим токеном Apify

3. Установите зависимости:
```bash
pip install -r requirements.txt
```

4. Запустите приложение:
```bash
python app.py
```

5. Откройте в браузере: http://localhost:5001

## Деплой

### Railway / Render / Heroku

1. Создайте репозиторий на GitHub
2. Подключите к платформе
3. Добавьте переменную окружения `APIFY_API_TOKEN`
4. Деплой запустится автоматически

### Dockerfile (опционально)

```dockerfile
FROM python:3.11-slim
WORKDIR /app
COPY requirements.txt .
RUN pip install -r requirements.txt
COPY . .
CMD ["gunicorn", "-b", "0.0.0.0:5000", "app:app"]
```

## API

### GET /api/account-url
Получить текущий URL аккаунта

### POST /api/account-url
Обновить URL аккаунта
```json
{ "url": "https://www.instagram.com/username/" }
```

### GET /api/data
Получить данные о Reels

Параметры:
- `start_date` - начальная дата (YYYY-MM-DD)
- `end_date` - конечная дата (YYYY-MM-DD)

